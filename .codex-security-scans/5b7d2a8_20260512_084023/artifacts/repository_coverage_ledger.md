# Repository Coverage Ledger

| Row | Boundary / Area | Family | Source / Boundary Checked | Sink / Control Checked | Candidate IDs | Disposition | Evidence Summary |
|---|---|---|---|---|---|---|---|
| RW-001 | Worker passcode auth | Default credentials / auth bypass | Internet client sends `x-team-passcode` | `resolvePasscodeRole()`, Wrangler vars, built client bundle | CAND-001 | reportable | `worker/index.ts:85-88` falls back to known passcodes; `wrangler.jsonc:15-18` deploys known passcodes as vars; `src/App.tsx:72-73` and `dist/assets/index-CN0lbbAX.js:71` expose same strings. |
| RW-002 | Worker object-level identity and data reads | Authz / IDOR / data exposure | Valid team passcode holder chooses `userId`; unauthenticated GET caller | API routes compare or persist body `userId`; GET routes bypass passcode | CAND-002, CAND-003, CAND-004, CAND-005 | reportable | Multiple write endpoints trust client-supplied user IDs; read endpoints are unauthenticated despite `VIEW_PASSCODE`. |
| RW-003 | Worker admin routes | Privilege escalation | Internet client with known admin passcode | `requireAdminPasscode()` on lineup, scores, next-ref admin, ladder refresh, push debug | CAND-001 | reportable | Admin role is only a passcode string, and that string is committed, deployed in config, and built into client code. |
| RW-004 | Worker D1 queries/schema | SQL injection and schema integrity | Request bodies, paths, query params; operator-applied schema | D1 `prepare().bind()` statements; seed/runtime constraints | CAND-009 | deferred | SQL injection suppressed because request values use bound parameters and dynamic SQL is not request-controlled. Schema mismatch deferred pending live D1/deploy-path evidence. |
| RW-005 | Worker outbound fetch | SSRF / callback abuse | Stored push endpoint, fixed Dribl URL | `fetch(endpoint)` in push, `fetch(DRIBL_LADDER_URL)` | CAND-006 | suppressed | Arbitrary public egress via push endpoint is possible, but internal metadata/private-network SSRF impact is not established; fixed Dribl URL is safe from attacker URL control. |
| RW-006 | Frontend rendering | Stored/reflected XSS | Chat text, names, map addresses, ladder fields | React rendering and iframe/image URL construction | none | suppressed | No raw HTML sinks found; React text rendering escapes content. Map iframe uses fixed Google Maps origin plus `encodeURIComponent`. |
| RW-007 | Browser service worker | Open redirect / notification click abuse | Push payload or pending queue payload | `client.navigate(targetUrl)` / `openWindow(targetUrl)` | none | suppressed | Payload URLs are produced by server as same-origin `/chat`; no route lets users store arbitrary notification URL. |
| RW-008 | CI/deploy | Supply-chain / deploy compromise | PR-controlled workflow code/package scripts | `npm run cf:deploy` with Cloudflare secrets | CAND-008 | reportable | Workflow runs on `pull_request`, checks out PR code, and deploys with Cloudflare secrets; fork secret withholding does not cover same-repo PRs. |
| RW-009 | Static public data | Sensitive public data exposure | Anonymous static asset fetch | JSON files under `public/data` copied to `dist/data` | CAND-007 | reportable | `dist/data` contains users, messages, availability, lineups, fines, events, nicknames. Sensitivity depends on whether data is real/stale/demo. |
